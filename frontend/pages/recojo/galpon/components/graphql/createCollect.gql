mutation createCollect($sectionId: ID!, $quantity: Int, $datetime: String ){
  createCollect(
    sectionId: $sectionId,
    quantity: $quantity,
    datetime: $datetime
  ){
    id
    quantity
    datetime
  }
}